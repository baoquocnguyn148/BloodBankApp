<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp">

    <!-- Icon -->
    <ImageView
        android:id="@+id/icon_bank"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_icon_yellow"
        android:padding="12dp"
        android:src="@drawable/ic_clipboard"
        app:tint="@color/accent_orange"
        android:contentDescription="Blood Bank Icon" />

    <!-- Cột Text -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Tiêu đề -->
        <TextView
            android:id="@+id/title_bank"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Blood Bank"
            android:textColor="@android:color/black"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Mô tả -->
        <TextView
            android:id="@+id/description_bank"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Manage inventory, handle requests, and coordinate donations."
            android:textColor="@android:color/darker_gray" />
    </LinearLayout>

</LinearLayout>
